# Some sample query

users.findOne(1) {
  username,
  email,

  # some dependant query
  shops.findOne {
    updated_at
  },

  addresses.findAll{*}
}

# SELECT `shops.updated_at` FROM shops WHERE user_id = 1;
# SELECT `users.username`, `users.email` FROM users WHERE id = 1;
# SELECT `addresses.XXX` FROM addresses WHERE user_id = 1 AND standard = 1;

